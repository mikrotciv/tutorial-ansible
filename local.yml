# Main config file
---
- name: Play for setting up workstation
  hosts: localhost
  connection: local
  become: true # Enables sudo

  tasks:
    - name: Include packages
      ansible.builtin.include_tasks: "{{ item }}"
      loop:
        - tasks/packages/development.yml
        - tasks/packages/utility.yml
        - tasks/packages/network.yml
